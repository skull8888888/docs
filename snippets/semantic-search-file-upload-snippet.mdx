You can either select a file or upload a file. We strongly suggest to select a file, if it has been uploaded before.
This is due to the fact that uploading a file automatically will chunk it and generate embeddings.